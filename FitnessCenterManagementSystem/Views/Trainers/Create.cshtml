@model FitnessCenterManagementSystem.Models.Trainer

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>
<h4>Trainer</h4>
<hr />
<div class="row">
    <div class="col-md-6">
        <form asp-action="Create" id="createTrainerForm">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group mb-3">
                <label asp-for="FullName" class="control-label">Ad Soyad</label>
                <input asp-for="FullName" class="form-control" />
                <span asp-validation-for="FullName" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="ServiceId" class="control-label">Hizmet Alanı</label>
                <select asp-for="ServiceId" class="form-control" asp-items="ViewBag.ServiceId"></select>
            </div>

            <div class="form-group mb-3">
                <label class="control-label fw-bold">Çalışma Günleri</label>
                <div class="card p-2 bg-light">
                    @{
                        string currentDays = Model?.WorkingDays ?? "";
                        string[] days = { "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday" };
                    }
                    <div class="row">
                        @foreach (var day in days)
                        {
                            <div class="col-md-4">
                                <div class="form-check">
                                    <input class="form-check-input day-checkbox" type="checkbox" value="@day">
                                    <label class="form-check-label">@day</label>
                                </div>
                            </div>
                        }
                    </div>
                </div>
                <input type="hidden" asp-for="WorkingDays" id="FinalWorkingDays" />
                <span asp-validation-for="WorkingDays" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="Expertise" class="control-label">Hakkında / Detay</label>
                <input asp-for="Expertise" class="form-control" />
                <span asp-validation-for="Expertise" class="text-danger"></span>
            </div>

            <div class="row">
                <div class="col-md-6 mb-3">
                    <label asp-for="StartTime" class="control-label">Başlangıç Saati</label>
                    <input asp-for="StartTime" class="form-control" type="time" />
                    <span asp-validation-for="StartTime" class="text-danger"></span>
                </div>
                <div class="col-md-6 mb-3">
                    <label asp-for="EndTime" class="control-label">Bitiş Saati</label>
                    <input asp-for="EndTime" class="form-control" type="time" />
                    <span asp-validation-for="EndTime" class="text-danger"></span>
                </div>
            </div>

            <div class="form-group mt-3">
                <input type="submit" value="Kaydet" class="btn btn-primary w-100" />
            </div>
        </form>
    </div>
</div>

<div class="mt-3">
    <a asp-action="Index">Listeye Dön</a>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        // DİKKAT: Artık ID ile seçiyoruz ("createTrainerForm")
        // Böylece Navbar'daki çıkış formuyla karışmaz.
        document.getElementById("createTrainerForm").addEventListener("submit", function () {
            var selected = [];
            document.querySelectorAll(".day-checkbox:checked").forEach(function (box) {
                selected.push(box.value);
            });
            document.getElementById("FinalWorkingDays").value = selected.join(",");
        });
    </script>
}